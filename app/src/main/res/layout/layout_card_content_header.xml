<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:id="@+id/llCardFragmentHeader"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	app:cardCornerRadius="0dp"
	app:layout_collapseMode="parallax"
	app:layout_collapseParallaxMultiplier="0.65"
	tools:cardBackgroundColor="@color/colorPrimary">

	<LinearLayout
		android:id="@+id/llCardFragmentHeaderContent"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="vertical"
		android:paddingStart="@dimen/std_padding"
		android:paddingEnd="@dimen/std_padding">

		<View
			android:layout_width="match_parent"
			android:layout_height="?attr/actionBarSize" />

		<FrameLayout
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:paddingBottom="@dimen/std_padding">

			<TextView
				android:id="@+id/txvCardFragmentName"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:fontFamily="sans-serif-medium"
				android:textColor="@color/white"
				android:textSize="20sp"
				android:textStyle="normal"
				tools:text="Имя карточки" />

			<EditText
				android:id="@+id/edxCardFragmentName"
				style="@style/EditableText"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:fontFamily="sans-serif-medium"
				android:textColor="@color/white"
				android:textSize="20sp"
				tools:text="Имя карточки" />
		</FrameLayout>

		<TextView
			android:id="@+id/txvCardFragmentPosition"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:paddingBottom="@dimen/std_padding"
			android:textColor="@color/white"
			android:textStyle="italic"
			tools:text="Название доски, Название списка" />
	</LinearLayout>
</androidx.cardview.widget.CardView>

