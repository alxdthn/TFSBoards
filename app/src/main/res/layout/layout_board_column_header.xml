<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:id="@+id/flColumnHeader"
	style="@style/Clickable"
	android:layout_width="match_parent"
	android:layout_height="@dimen/column_header_height"
	android:background="@drawable/column_header_background">

	<TextView
		android:id="@+id/txvColumnHeaderName"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:gravity="center_vertical"
		android:fontFamily="sans-serif-medium"
		android:padding="12dp"
		android:textColor="#555555"
		android:textSize="18sp"
		tools:text="Название колонки" />

	<EditText
		android:id="@+id/edxColumnHeaderInput"
		style="@style/EditableText"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:gravity="center_vertical"
		android:fontFamily="sans-serif-medium"
		android:padding="12dp"
		android:textColor="#555555"
		android:textSize="18sp"
		android:visibility="gone"
		tools:text="Название колонки"
		tools:visibility="visible" />
</FrameLayout>

